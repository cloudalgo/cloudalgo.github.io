---
import StatsCounter from '../ui/StatsCounter.tsx';
---
<div class="counts">
  {/* Any wrapper the page needs stays here, outside the island: a class
      written onto a node React owns is a hydration race. */}
  <div class="container">
    <StatsCounter client:load />
  </div>
</div>
