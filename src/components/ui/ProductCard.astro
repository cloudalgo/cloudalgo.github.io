---
import type { CollectionEntry } from 'astro:content';
import Button from './Button.astro';

interface Props {
  product: CollectionEntry<'products'>;
  featured?: boolean;
  dark?: boolean;
}

const { product, featured: _featured = false, dark = false } = Astro.props;
const { data } = product;
const slug = product.id;

const statusLabel = (s: string) =>
  s === 'ga' ? 'Generally Available' : s === 'preview' ? 'Preview' : 'Beta';
const isEarlyAccess = data.status === 'preview' || data.status === 'beta';
const typeLabel = (t: string) =>
  t === 'salesforce-app' ? 'Salesforce App' : t === 'mobile-app' ? 'Mobile App' : t === 'desktop-app' ? 'Desktop App' : 'Integration';
const orderNum = String(data.order).padStart(2, '0');

// Small icon SVGs for dark variant cards
const productIcons: Record<string, string> = {
  'algobridge': `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="1" y="4" width="8" height="16" rx="2" stroke="currentColor" stroke-width="1.5"/>
    <rect x="15" y="4" width="8" height="16" rx="2" stroke="currentColor" stroke-width="1.5"/>
    <path d="M9.5 9 L14.5 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    <path d="M13 7.5 L14.5 9 L13 10.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    <path d="M14.5 15 L9.5 15" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    <path d="M11 13.5 L9.5 15 L11 16.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>`,
  'sf-sync-connector': `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="1" y="7" width="8" height="10" rx="2" stroke="currentColor" stroke-width="1.5"/>
    <rect x="15" y="7" width="8" height="10" rx="2" stroke="currentColor" stroke-width="1.5"/>
    <path d="M9.5 10 L14.5 10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    <path d="M13 8.5 L14.5 10 L13 11.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    <path d="M14.5 14 L9.5 14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    <path d="M11 12.5 L9.5 14 L11 15.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>`,
  'insurealgo': `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="5" y="1" width="14" height="22" rx="3" stroke="currentColor" stroke-width="1.5"/>
    <path d="M9 5 L15 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    <path d="M12 9 L12 9.01" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    <path d="M9 13 L12 15 L15 11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    <circle cx="12" cy="20" r="1" fill="currentColor"/>
  </svg>`,
  'pledgivo': `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M12 21C12 21 3 14.5 3 8.5C3 6 5 4 7.5 4C9.24 4 10.75 4.96 11.56 6.35C11.75 6.67 12.25 6.67 12.44 6.35C13.25 4.96 14.76 4 16.5 4C19 4 21 6 21 8.5C21 14.5 12 21 12 21Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/>
    <path d="M8 11 L10.5 13.5 L16 8.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>`,
  'orgvitals': `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="2" y="3" width="20" height="16" rx="2.5" stroke="currentColor" stroke-width="1.5"/>
    <path d="M2 7 L22 7" stroke="currentColor" stroke-width="1.5"/>
    <circle cx="4.6" cy="5" r="0.7" fill="currentColor"/>
    <circle cx="6.8" cy="5" r="0.7" fill="currentColor"/>
    <path d="M5 13 L8.5 13 L10.5 10 L13 16 L15 13 L19 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    <path d="M8.5 22 L15.5 22" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
  </svg>`,
};
const iconSvg = productIcons[slug] ?? productIcons['cloudalgo-flow-audit'];


---

{dark ? (
  <div class="product-card-dark" style="background:var(--color-paper-a06);border:1px solid var(--color-paper-a12);border-radius:12px;padding:1.75rem;display:flex;gap:1.25rem;align-items:flex-start;height:100%;">
    <div style="width:44px;height:44px;border-radius:10px;background:var(--color-paper-a08);border:1px solid var(--color-paper-a15);flex-shrink:0;display:flex;align-items:center;justify-content:center;color:var(--color-on-inverse);overflow:hidden;">
      <Fragment set:html={iconSvg} />
    </div>
    <div style="flex:1;min-width:0;display:flex;flex-direction:column;height:100%;">
      <div style="display:flex;align-items:center;gap:0.5rem;margin-bottom:0.5rem;flex-wrap:wrap;">
        {data.status === 'ga' ? (
          <span style="background:var(--color-paper-a90);color:var(--color-heading);font-size:0.6rem;font-weight:700;padding:2px 8px;border-radius:100px;text-transform:uppercase;letter-spacing:0.06em;">
            {statusLabel(data.status)}
          </span>
        ) : (
          <span style="border:1px solid var(--color-paper-a40);color:var(--color-on-inverse-strong);font-size:0.6rem;font-weight:700;padding:2px 8px;border-radius:100px;text-transform:uppercase;letter-spacing:0.06em;">
            {statusLabel(data.status)}
          </span>
        )}
        {isEarlyAccess && (
          <span style="font-size:0.65rem;color:var(--color-on-inverse-a40);">Early access</span>
        )}
      </div>
      <div style="font-size:1rem;font-weight:700;color:var(--color-on-inverse);margin-bottom:0.35rem;">{data.title}</div>
      <div style="font-size:0.85rem;color:var(--color-on-inverse-muted);line-height:1.6;margin-bottom:1rem;">{data.excerpt}</div>
      <a href={`/products/${slug}`} style="font-size:0.8rem;font-weight:700;color:var(--color-on-inverse-a80);text-decoration:underline;text-underline-offset:3px;transition:color 0.2s;margin-top:auto;" class="prod-card-link-dark">
        Learn more &rarr;
      </a>
    </div>
  </div>
) : (
  <article class="card card--radius-2xl card--lift prod-card">
    <!-- Card body -->
    <div class="prod-card-body">
      <div class="prod-card-header">
        <span class="prod-card-index">{orderNum}</span>
        <span class="prod-card-dot">&middot;</span>
        <span class="prod-card-type">{typeLabel(data.type)}</span>
      </div>
      <h2 class="prod-card-title">{data.title}</h2>
      <div style="margin-bottom:0.75rem;">
        {data.status === 'ga' ? (
          <span style="background:var(--surface-inverse);color:var(--color-on-inverse);font-size:0.6rem;font-weight:700;padding:2px 8px;border-radius:100px;text-transform:uppercase;letter-spacing:0.06em;">
            {statusLabel(data.status)}
          </span>
        ) : (
          <span style="border:1.5px solid var(--border-strong);color:var(--color-heading);font-size:0.6rem;font-weight:700;padding:2px 8px;border-radius:100px;text-transform:uppercase;letter-spacing:0.06em;">
            {statusLabel(data.status)}
          </span>
        )}
        {isEarlyAccess && (
          <span style="font-size:0.65rem;color:var(--color-text);margin-left:0.4rem;">Early access</span>
        )}
      </div>
      <p class="prod-card-excerpt">{data.excerpt}</p>
      <ul class="prod-card-features">
        {data.features.slice(0, 3).map(f => (
          <li>
            <svg width="16" height="16" viewBox="0 0 18 18" fill="none" aria-hidden="true">
              <rect x="0.75" y="0.75" width="16.5" height="16.5" rx="4.25" stroke="var(--illus-ink)" stroke-width="1.5"/>
              <path d="M5.5 9L7.8 11.5L12.5 6.5" stroke="var(--illus-ink)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span>{f.title}</span>
          </li>
        ))}
      </ul>
      <div class="prod-card-ctas">
        <Button href={`/products/${slug}`} variant="primary" size="xs">
          Learn more &rarr;
        </Button>
        {data.externalUrl ? (
          <Button href={data.externalUrl} variant="outline" size="xs" external>
            {data.type === 'desktop-app' ? 'Download ↗' : 'Get started ↗'}
          </Button>
        ) : (
          <Button href="/contact" variant="outline" size="xs">
            Join waitlist
          </Button>
        )}
      </div>
    </div>
  </article>
)}

<style>
/* Shell and hover come from `.card card--radius-2xl card--lift`, the
   same three classes the case-study card carries. */
.prod-card {
  overflow: hidden;
  display: flex;
  flex-direction: column;
  height: 100%;
}

.prod-card-body {
  padding: 1.75rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}
.prod-card-header {
  display: flex;
  align-items: center;
  gap: 0.375rem;
  margin-bottom: 0.625rem;
}
.prod-card-index {
  font-size: 0.7rem;
  font-weight: 700;
  color: var(--color-heading);
  letter-spacing: 0.04em;
}
.prod-card-dot {
  font-size: 0.7rem;
  color: var(--color-text-faintest);
}
.prod-card-type {
  font-size: 0.7rem;
  font-weight: 600;
  color: var(--color-text);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.prod-card-title {
  font-size: 1.25rem;
  font-weight: 800;
  color: var(--color-heading);
  letter-spacing: -0.02em;
  line-height: 1.2;
  margin-bottom: 0.625rem;
}
.prod-card-excerpt {
  font-size: 0.9rem;
  color: var(--color-text);
  line-height: 1.65;
  margin-bottom: 1rem;
  flex: 1;
}
.prod-card-features {
  list-style: none;
  padding: 0;
  margin: 0 0 1.25rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.prod-card-features li {
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
}
.prod-card-features li svg { flex-shrink: 0; margin-top: 0.1rem; }
.prod-card-features li span {
  font-size: 0.875rem;
  color: var(--color-text);
  line-height: 1.5;
}
.prod-card-ctas {
  display: flex;
  gap: 0.625rem;
  flex-wrap: wrap;
  margin-top: auto;
}
.prod-card-link-dark:hover { color: var(--color-on-inverse) !important; }
</style>
